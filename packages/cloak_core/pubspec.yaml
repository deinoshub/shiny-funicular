name: cloak_core
description: Pure-Dart core for CloakManager (stealth args, launcher, CDP, binary management).
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
